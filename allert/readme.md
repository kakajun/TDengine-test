# 数据告警系统 (Data Alert System)

这是一个基于 Python 的高性能工业数据告警系统，旨在处理 CSV 格式的时序数据，支持基于规则的实时告警和机器学习模型辅助诊断。

## 🚀 技术栈

*   **语言**: Python 3.8+
*   **核心处理**: `pandas` (高性能向量化计算), `numpy`
*   **规则引擎**: `pandas.eval` / `numexpr` (支持复杂布尔表达式)
*   **机器学习**: `lightgbm` (优先), `scikit-learn` (RandomForest 作为回退), `joblib`
*   **CLI 工具**: `click`
*   **配置管理**: `PyYAML`
*   **日志**: `loguru`
*   **编码检测**: `chardet`

## ✨ 核心功能

1.  **健壮的数据加载 (Data Loader)**
    *   自动检测文件编码 (GBK/UTF-8)。
    *   自动解析复合主键 (`PK` 列 -> `device_id` + `timestamp`)。
    *   自动展开位域特征 (`bit` 列 -> `bit_0`, `bit_1`...)。
    *   支持中英文列名映射 (基于 `mapping_loader`)。

2.  **向量化规则引擎 (Rule Engine)**
    *   支持使用中文列名编写规则表达式 (如 `支路电流 > 0.3 and 总辐照度 > 200`)。
    *   **时间窗口支持**: 支持 `5m any` (5分钟内任意触发), `10m all` (10分钟持续触发) 等逻辑。
    *   **告警去重**: 支持基于时间窗口的告警抑制 (Deduplication)，避免重复骚扰。

3.  **机器学习闭环 (Model Loop)**
    *   **数据合成**: 内置数据合成器，基于统计特征自动生成正负样本用于冷启动训练。
    *   **自动训练**: 集成 LightGBM/RandomForest，支持模型保存与加载。

## 📂 目录结构

```text
alert-system/
├── configs/                # 配置文件目录
│   ├── config.yaml         # 系统主配置 (数据路径、输出路径等)
│   ├── rules.yaml          # 告警规则定义
│   └── test_config.yaml    # 测试配置
├── allert/                    # 源代码
│   ├── model/              # 模型相关代码 (训练、合成)
│   ├── alert_runner.py     # [入口] CLI 主程序
│   ├── data_loader.py      # 数据加载与预处理
│   ├── mapping_loader.py   # 列名映射加载
│   └── rule_engine.py      # 规则引擎核心
├── out/                    # 输出目录 (告警结果、模型文件)

```

## 🛠️ 安装说明

1.  **环境准备**
    建议使用 Conda 创建独立环境：
    ```bash
    conda create -n alert_sys python=3.9
    conda activate alert_sys
    ```

2.  **安装依赖**
    ```bash
    pip install -r requirements.txt
    ```

## 📖 使用指南

### 1. 配置文件

在 `configs/config.yaml` 中配置数据源和规则路径：

```yaml
data:
  mapping_path: "靳家梁.csv"  # 映射表路径
  mapping_encoding: "gbk"

rules:
  path: "configs/rules.yaml"

output:
  path: "out/alerts.csv"
```

在 `configs/rules.yaml` 中定义告警规则：

```yaml
- name: 支路电流异常
  expr: 支路电流 < 0.3 and 总辐照度 > 200
  severity: high
  window: 5m any      # 可选: 5分钟内任意一次满足即告警
  dedup: 10m          # 可选: 10分钟内不重复告警
  message: "支路电流偏低，请检查"
```

### 2. 运行告警分析

使用 `run` 命令对 CSV 文件进行批量分析：

```bash
# 基本用法
python -m src.alert_runner run --input your_data.csv

# 指定配置文件
python -m src.alert_runner run --config configs/config.yaml --input your_data.csv
```

运行后，结果将保存在 `out/alerts.csv` (或配置中指定的路径)。

### 3. 训练模型 (Demo)

使用 `train-model` 命令基于输入数据合成样本并训练分类模型：

```bash
python -m src.alert_runner train-model --config configs/config.yaml --input your_data.csv
```

模型将保存为 `out/model.pkl`。

## 🧪 开发与扩展

*   **添加新规则**: 直接修改 `rules.yaml`，无需重启代码。
*   **扩展模型**: 在 `src/model/` 下继承 `BaseModel` 实现新算法。
*   **日志**: 系统使用 `loguru`，日志默认输出到控制台，可在代码中配置输出到文件。
